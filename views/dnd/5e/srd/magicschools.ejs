<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>D&D | 5e | SRD | Magic Schools | Far Reach Co.</title>
    <link rel="stylesheet" href="/style.css" />
    <link rel="stylesheet" href="/info.css" />
    <script src="/lib/MobileNavHandler.js" defer></script>
    <%- include('../../../partials/icons') %>
  </head>
  <%- include('../../../partials/google') %>
  <body>
    <%- include('../../../partials/nav') %>
    <div class="container-fluid p-3 info-container">
      <%- include('../../../partials/dnd/srdintro') %>
      <br />
      <div class="inline-div">
        <small>
          <a href="/dnd/5e/srd/contents">Contents</a> ->
          <div style="color: var(--green)">Magic Schools</div>
        </small>
      </div>
      <hr />
      <h2>Magic Schools</h2>
      <ul>
        <% data.forEach(function(school){ %>
        <li>
          <a href="#<%= school.index %>"><%= school.name %></a>
        </li>
        <% }); %>
      </ul>
      <br />
      <% data.forEach(function(school){ %>
      <div class="inline-div" id="<%= school.index %>">
        <h3><%= school.name %></h3>
      </div>
      <div class="indent">
        <p><%= school.desc %></p>
      </div>
      <br />
      <% }); %>
    </div>
    <%- include('../../../partials/footer') %>
  </body>
</html>
