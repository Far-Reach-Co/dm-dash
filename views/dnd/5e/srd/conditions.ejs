<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>D&D | 5e | SRD | Conditions | Far Reach Co.</title>
    <link rel="stylesheet" href="/style.css" />
    <link rel="stylesheet" href="/info.css" />
    <script src="/lib/MobileNavHandler.js" defer></script>
    <%- include('../../../partials/icons') %>
  </head>
  <%- include('../../../partials/google') %>
  <body>
    <%- include('../../../partials/nav') %>
    <div class="container-fluid p-3 info-container">
      <%- include('../../../partials/dnd/srdintro') %>
      <br />
      <div class="inline-div">
        <small>
          <a href="/dnd/5e/srd/contents">Contents</a> ->
          <div style="color: var(--green)">Conditions</div>
        </small>
      </div>
      <hr />
      <h2>Conditions</h2>
      <ul>
        <% data.forEach(function(condition){ %>
        <li>
          <a href="#<%= condition.index %>"><%= condition.name %></a>
        </li>
        <% }); %>
      </ul>
      <br />
      <% data.forEach(function(condition){ %>
      <div class="inline-div" id="<%= condition.index %>">
        <h3><%= condition.name %></h3>
      </div>
      <div class="indent">
        <% condition.desc.forEach((str, index) => { %>
        <p><%= str %></p>
        <% }) %>
        <br />
      </div>
      <br />
      <% }); %>
    </div>
    <%- include('../../../partials/footer') %>
  </body>
</html>
